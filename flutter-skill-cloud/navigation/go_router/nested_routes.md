# Nested Routes

## Patterns

- Use nested routes to model tabs/sections.
- Keep each nested navigator’s responsibility clear (which stack owns which pages).

## Pitfalls

- Back button behavior becomes inconsistent without a deliberate design.

