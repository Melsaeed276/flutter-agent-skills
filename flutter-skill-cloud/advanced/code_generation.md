# Code Generation

## Patterns

- Use generation to remove repetitive boilerplate (DTOs, unions, serialization).
- Keep generated code out of core logic; treat it as an implementation detail.

## Pitfalls

- Hard-to-debug build steps when generators are misconfigured.

