# Flavors

## Patterns

- Separate config by environment (API base URL, feature flags) without duplicating code.
- Keep flavor selection near app entry point and dependency wiring.

## Pitfalls

- Environment-specific logic scattered across widgets.

