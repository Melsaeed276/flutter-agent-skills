# CI/CD

## Patterns

- Build the same way locally and in CI (same Flutter/Dart versions).
- Run fast tests on every change; run heavier suites on a schedule or before release.

## Pitfalls

- CI-only scripts that aren’t documented or reproducible.

